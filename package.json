{
  "name": "@hyperapp/logger",
  "version": "0.5.0",
  "description": "Log Hyperapp state updates and action information to the console",
  "main": "dist/withLogger.js",
  "jsnext:main": "src/index.js",
  "module": "src/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hyperapp/logger.git"
  },
  "files": [
    "src/**",
    "dist/**"
  ],
  "scripts": {
    "test": "jest --coverage --no-cache",
    "build": "npm run bundle && npm run minify",
    "bundle": "rollup -i src/index.js -o dist/withLogger.js -m -f umd -n withLogger",
    "minify": "uglifyjs dist/withLogger.js -o dist/withLogger.js -mc pure_funcs=['Object.defineProperty'] --source-map includeSources,url=withLogger.js.map",
    "prepare": "npm run build",
    "format": "prettier --semi false --write '{src,test}/**/*.js'"
  },
  "babel": {
    "presets": "env"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "hyperapp": "^1.1.2",
    "jest": "^21.2.1",
    "prettier": "^1.11.1",
    "rollup": "^0.56.3",
    "uglify-js": "^3.3.12"
  },
  "peerDependencies": {
    "hyperapp": "^1.1.2"
  },
  "author": "Wolfgang Wedemeyer <wolf@okwolf.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/hyperapp/logger/issues"
  },
  "homepage": "https://github.com/hyperapp/logger"
}
